import { defineMessages } from "react-intl";

export default defineMessages({
  title: {
    id: "tourism.title",
    defaultMessage: "Tourism (default)"
  },
  address: {
    id: "tourism.address",
    defaultMessage: "Address (default)"
  }
});
