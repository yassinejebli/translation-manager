import { defineMessages } from "react-intl";

export default defineMessages({
  title: {
    id: "news.title",
    defaultMessage: "News (default)"
  },
  date: {
    id: "news.date",
    defaultMessage: "Date (default)"
  }
});
